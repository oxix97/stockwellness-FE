# [긴급] 프론트엔드 런타임 오류 및 빌드 자산 누락 수정 요청

현재 운영 환경(`stockwellness.duckdns.org`)에서 발생한 프론트엔드 오류로 인해 사이트 접속 시 앱이 중단되는 현상이 발견되었습니다. 인프라 측에서 임시 패치를 진행했으나, 소스 코드 수준에서의 정식 수정 및 재배포를 요청드립니다.

## 1. 주요 오류 내용

### A. ReferenceError: Search is not defined
- **현상**: 메인 번들 파일 실행 중 `Search` 컴포넌트를 참조하려 했으나 정의되지 않아 JS 실행이 중단됨.
- **발생 위치**: 라우터 설정 부분
  ```javascript
  // index-C5KH9Glp.js 내 실제 코드 예시
  { path: "search", Component: Search }
  ```
- **원인 추정**: 
  - `Search` 컴포넌트의 `import` 문이 누락되었거나 오타가 발생함.
  - 빌드 도구(Vite)의 트리 쉐이킹 과정에서 의도치 않게 삭제되었을 가능성.
- **임시 조치**: 운영 서버 컨테이너 내 JS 파일 첫 줄에 `var Search = () => null;`을 강제 주입하여 크래시만 방지함.

### B. PWA 매니페스트 아이콘 누락 (404 Not Found)
- **현상**: 브라우저 콘솔에 `pwa-192x192.png` 및 `pwa-512x512.png` 다운로드 실패 에러 발생.
- **원인**: `manifest.webmanifest`에는 아이콘이 명시되어 있으나, 빌드 결과물(`dist/`)에 해당 이미지 파일들이 포함되지 않음.
- **임시 조치**: 컨테이너 내부 해당 경로에 빈(0바이트) 파일을 생성하여 다운로드 에러만 차단함.

## 2. 수정 요청 사항

1.  **라우터 설정 점검**: `Search` 컴포넌트가 올바르게 임포트되어 있는지, 그리고 `Component` 속성에 정상적으로 할당되었는지 확인해 주세요.
2.  **PWA 빌드 설정 확인**: `vite-plugin-pwa` 설정을 확인하여 `public` 폴더의 아이콘들이 `dist` 폴더로 정상 복사되는지 점검해 주세요.
3.  **서비스 워커(sw.js) 확인**: 현재 인프라 설정에서 `/sw.js`에 대한 캐시 무효화 처리를 강화했습니다. PWA 업데이트 로직이 정상적인지 로컬에서 검증 바랍니다.

## 3. 재배포 안내
- 위 사항 수정 후 새로운 이미지 태그를 생성하여 GitHub 배포 웹훅을 트리거해 주세요.
- 배포 시 인프라의 `green` 슬롯에 우선 적용하여 동작 확인 후 전환될 예정입니다.

---
**인프라 담당자 메모**: 현재 `green` 컨테이너에 임시 패치가 적용된 상태이므로, 다음 배포 시 해당 패치는 덮어씌워지게 됩니다. 반드시 소스 코드 수정을 거친 정식 빌드본이 배포되어야 합니다.
